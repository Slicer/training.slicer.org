title: Slicer Training Compendium
author:
  name: GitHub User
  email: your-email@domain.com

# The `>` after `description:` means to ignore line-breaks until next key.
# If you want to omit the line-break after the end of text, use `>-` instead.
description: >
  Collection of training materials for 3D Slicer.

baseurl: "" # the subpath of your site, e.g. /blog
url: "https://training.slicer.org" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: 3DSlicerApp
github_username:  Slicer

# Build settings
theme: jekyll-theme-primer

plugins:
  - jekyll-readme-index

readme_index:
  remove_originals: true

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
  # This project
  - LICENSE.md
  - netlify.toml
